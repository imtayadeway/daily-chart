<%= form_for :submissions, url: submissions_path do |f| %>
  <% @chart.items.each do |item| %>
    <%= f.label item.name %>
    <%= f.check_box item.name %>
  <% end %>

  <%= f.submit "Submit" %>
<% end %>
